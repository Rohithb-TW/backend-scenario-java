apiVersion: v1
kind: Service
metadata:
  name: petclinic
  namespace: petclinic
  labels:
    app: petclinic
spec:
  selector:
    app: petclinic
  ports:
    - name: http
      protocol: TCP
      port: 9966
      targetPort: 9966
  type: ClusterIP
